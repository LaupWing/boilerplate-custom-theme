# 2026-04-04

## Next up
- **Router decision: two branches**
  - `main` — simple prefix-only Router (like Antiquewarehouse: `/en/over-ons/`, no slug translation, ~100 LOC)
  - `feature/translated-slugs` — current Router with full slug resolution (405 LOC, DB lookups, segment-by-segment resolution)
  - Strip `main` down to prefix-only, move current Router to the feature branch

- **One day deep-dive into the rewrite system (before deciding to keep or nuke translated slugs)**
  - Go line by line through Router.php
  - Document every rewrite rule, every workaround, every "why"
  - Understand: attachment fallback, priority-1 intercept, segment resolution, rewrite version tracking
  - Write it all into CLAUDE.md so future you can debug confidently
  - After that day: decide if translated slugs are worth offering to clients

## Future
- Custom update server — theme checks your own server for updates instead of wordpress.org
- Marketplace admin page — React page listing available modules with activate/deactivate
- Convert admin pages to React where it makes sense
