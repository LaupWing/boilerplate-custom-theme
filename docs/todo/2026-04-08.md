# Todo — 2026-04-08

## Next up (tomorrow)
- Finish reading: `Processor.php` (process_batch method) and `tracking/Controller.php`
- Wire up Dashboard with real stats from DB
- Save recipient tags from editor sidebar to post meta
- Save preview text from editor sidebar to post meta
- Test full send flow: publish → queue → deliver → track opens/clicks
- Set up SES SNS webhook for bounce handling
- Clean up unused `$inject_tracking` variable in Processor.php

## Documentation
- Add CLAUDE.md to `inc/adapters/` — how the adapter pattern works, how to add a new provider
- Add CLAUDE.md to `inc/queue/` — how the queue works, batch processing, cron
- Add CLAUDE.md to `inc/tracking/` — endpoints, pixel, click redirect, unsubscribe, webhook
- Add CLAUDE.md to `inc/sender/` — email template, HTML conversion, custom block rendering
- Add CLAUDE.md to `inc/subscribers/` — MVC structure, REST endpoints
- Add CLAUDE.md to `inc/campaigns/` — CPT, status mapping, meta fields
- Add CLAUDE.md to plugin root — overall architecture, module overview

## Future
- Add JSDoc type definitions (Subscriber, Campaign, Settings) for type safety
- AI name extraction endpoint (OpenAI integration)
- Adapter: SendGrid implementation
- Adapter: Postmark implementation
- Warm-up mode (gradual batch size increase for new domains)
- Subscriber detail: real tracking stats (emails received, opens, clicks)
- Campaign detail stats page (per-subscriber open/click data)
